package com.clepsy.android.capture

/**
 * Legacy screen capture implementation removed in favour of app usage events.
 * The file is intentionally left blank to avoid compilation errors while the
 * package is phased out.
 */
